'use strict';

const categories = [{name: `Книги`}, {name: `Посуда`}, {name: `Игры`}, {name: `Разное`}];

const users = [
  {name: `Ivan`, surname: `Ivanov`, email: `ivan@mail.com`, password: `UXhDWA`, avatar: `ivan_avatar.jpg`},
];

const types = [{name: `Куплю`}, {name: `Продам`}];

module.exports = {
  categories,
  users,
  types,
};
